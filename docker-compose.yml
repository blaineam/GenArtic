version: "3"
services:
  genartic:
    env_file:
      - .env
    ports:
      - "8873:8873"
    restart: always 
    container_name: genartic
    image: genartic
    build: 
      context: . 
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]